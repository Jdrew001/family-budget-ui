import { ViewportHeightDirective } from './viewport-height.directive';

describe('ViewportHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewportHeightDirective();
    expect(directive).toBeTruthy();
  });
});
